import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  card: {
    position: 'absolute',
  },
  container: {
    flexDirection: 'row',
    height: 300,
    alignSelf: 'center',
    width: 400,
  },
});

export {styles};
